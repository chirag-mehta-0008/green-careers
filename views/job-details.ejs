<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 font-sans min-h-screen">

    <div class="container mx-auto px-4 py-10 max-w-3xl">
        <a href="/" class="inline-flex items-center text-emerald-700 hover:text-emerald-900 mb-6 transition font-bold">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
            Back to Jobs
        </a>

        <% if (job) { %>
            <div class="bg-white rounded-2xl shadow-xl overflow-hidden border border-gray-100">
                
                <div class="bg-emerald-800 p-8 text-white relative overflow-hidden">
                    <div class="absolute top-0 right-0 -mt-4 -mr-4 w-24 h-24 bg-emerald-700 rounded-full opacity-50"></div>
                    <h1 class="text-2xl md:text-3xl font-bold relative z-10"><%= job.job_title %></h1>
                    <div class="mt-4 flex flex-wrap gap-2 text-emerald-50 text-sm relative z-10">
                        <span class="bg-emerald-700 px-3 py-1 rounded-full border border-emerald-600"><%= job.employer_name %></span>
                        <span class="bg-emerald-700 px-3 py-1 rounded-full border border-emerald-600"><%= job.job_city || 'Remote' %>, <%= job.job_country %></span>
                        <span class="bg-emerald-700 px-3 py-1 rounded-full border border-emerald-600"><%= job.job_employment_type || 'Full Time' %></span>
                    </div>
                </div>

                <div class="p-8">
                    <a href="<%= job.job_apply_link %>" target="_blank" class="block w-full bg-emerald-600 hover:bg-emerald-700 text-white text-center py-4 rounded-xl font-bold text-lg mb-8 transition shadow-lg transform hover:-translate-y-1">
                        Apply for this Role 🚀
                    </a>

                    <hr class="border-gray-100 mb-8">

                    <div class="prose prose-emerald max-w-none text-gray-700 leading-relaxed space-y-4">
                        <h3 class="text-xl font-bold text-emerald-900">About the Role</h3>
                        <div class="whitespace-pre-line"><%- job.job_description %></div>
                    </div>

                    <div class="mt-10 grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="bg-emerald-50 p-4 rounded-lg border border-emerald-100">
                            <span class="text-xs text-emerald-600 uppercase tracking-wide font-bold">Posted On</span>
                            <p class="font-bold text-gray-800 mt-1"><%= job.job_posted_at_datetime_utc ? new Date(job.job_posted_at_datetime_utc).toDateString() : 'N/A' %></p>
                        </div>
                        <div class="bg-emerald-50 p-4 rounded-lg border border-emerald-100">
                            <span class="text-xs text-emerald-600 uppercase tracking-wide font-bold">Publisher</span>
                            <p class="font-bold text-gray-800 mt-1"><%= job.job_publisher || 'LinkedIn' %></p>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="bg-white p-10 rounded-xl shadow text-center">
                <h2 class="text-xl font-bold text-red-500">Job Not Found</h2>
                <p class="text-gray-500 mt-2">The job might have expired.</p>
            </div>
        <% } %>
    </div>
</body>
</html>